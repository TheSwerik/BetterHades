<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="BetterHades.MainWindow"
        Title="BetterHades">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1.25*" />
        </Grid.RowDefinitions>
        <!-- <CheckBox Grid.Row="1" Grid.Column="1" Click="CheckboxOnClick" IsChecked="False" /> -->


        <!-- <CheckBox Grid.Row="2" Grid.Column="1" Click="CheckboxOnClick" IsChecked="False" /> -->
        <!-- <TextBlock Grid.Row="1" Grid.Column="2" Width="40" Height="40" Background="Gray" /> -->
        <!-- <TextBlock Grid.Row="2" Grid.Column="2" Width="40" Height="40" Background="Gray" /> -->
        <Canvas Grid.Column="1" Grid.Row="1" DragDrop.AllowDrop="True" />
    </Grid>
</Window>